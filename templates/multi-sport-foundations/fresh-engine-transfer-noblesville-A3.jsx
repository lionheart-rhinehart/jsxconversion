// fresh-engine-transfer-noblesville-A3 — STATIC (image) ugc-selfie cold hook.
// Media-backed: full-bleed athlete still + bold Anton hook over a legibility scrim.
// _FONT_PREFLIGHT: Anton, Geist, JetBrains Mono
import config from './fresh-engine-transfer-noblesville-A3.config.json';
import { LayerStack } from './_helpers.jsx';

export default function FreshEngineTransferNoblesvilleA3() {
  return <LayerStack config={config} />;
}
