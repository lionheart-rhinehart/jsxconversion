# Big regenerable artifacts — kept OUT of git (and out of the index) per the contract:
# the index references the vectors; it never embeds them. Re-create with run.mjs.
embeddings.vectors.npz
__pycache__/
# Gemini key (local only — never commit)
.env
# viewer runtime scratch (local only)
.viewer.log
.shot.mjs
.gallery-preview.png
.editor-preview.png
.clusters.png
.heatmap.png
.mediatest.png
# Media-test harness: COMMIT the scripts (reusable methodology), IGNORE the heavy
# regenerable outputs (renders, embeddings, model-derived artifacts).
_experiment/runs/
_experiment/_work/
_experiment/duotone/
_experiment/cutout/
_experiment/compare/
_experiment/*.png
_experiment/results.json
_experiment/render-manifest.json
_experiment/embeddings.vectors.npz
_experiment/assets/
_experiment/*.jpg
_experiment/*.jsx
_experiment/labels.json
