# ⛔ creative-engine-v1 — ARCHIVED, reference-only

**This is the retired old creative system.** It did not work end-to-end and is kept ONLY as a
reference to mine for ideas. The **active** system is `/creative-engine` (skill at
`.claude/skills/creative-engine/`, code at the project-root `creative-engine/` folder).

## Rules

- **Do NOT import from this folder.** Nothing live may depend on it.
- **Do NOT edit it** (it is in the locked zone `creative-v1` — `/unlock-creative-v1` only if you
  truly must, e.g. to read with tooling that needs write).
- Take *ideas* from here; take *code* only when it passes a clean test in the new home.

## What's archived here (former `.claude/skills/` skills)

- `creative-engine/` — old campaign orchestrator
- `compose-creative/` — old from-scratch composer
- `repurpose-campaign/` — old brand re-skin
- `creative-editor/` — old design→editor (the lossy flatten/rebuild that caused the churn)
- `design-to-approval/` — old approval sender
- `jsx-to-mp4/` — old atomic renderer (best *render* reference; still don't import)

**Why retired:** see master plan `C:\Users\lionh\.claude\plans\so-i-see-that-memoized-parnas.md`
("Context" section) — AI judgment in mechanical paths (inconsistency) + lossy rebuilds (infidelity).
